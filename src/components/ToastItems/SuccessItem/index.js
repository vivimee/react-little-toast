/* eslint-disable */

import React from 'react';
import './index.less';

export default ({ message }) => {
    return (
        <div>
            <img />
            <div>{message}</div>
        </div>
    );
};
